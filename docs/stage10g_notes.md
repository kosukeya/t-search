# Stage 10G Notes — Synthesis and Evidence-Selected Stage 11 Gate

Status: **Stage 10G completed; criteria 48–49 satisfied. Criterion 50 remains external.**

## Synthesis question

Stage 10G asks whether the Stage 10A–F evidence supports one of the four outcomes frozen in the protocol:

- `measurement_covariant`;
- `measurement_partial`;
- `measurement_obstructed`;
- `inconclusive`.

The synthesis is computed from the executable Stage 10A–F diagnostics rather than from documentation-only assertions.

## Executable synthesis outcome

The selected outcome is:

`measurement_covariant`

for the declared scope:

**fully typed / continuation-aware / finite A/B/C clock atlas / future-signature measurement family**.

The positive chain is cumulative:

1. Stage 10A reproduces the Stage 9C reference outcomes, effects, and likelihoods while making anchor/target/outcome/continuation typing explicit.
2. Stage 10B independently lifts the measurement into each continuation and selects the reference-induced operational normalization/effect-form representation.
3. Stage 10C establishes representation covariance over 18 charts, 108 genuine distinct-clock transports, and 324 three-clock compositions.
4. Stage 10D establishes per-continuation probability/completeness/positivity covariance, including 196 Hermitian-tomography-complete valid probes and 7056 probe outcome-probability evaluations.
5. Stage 10E restores continuation weights, matched epistemic/ontic-extension public views, hidden-selector controls, and common-evidence updates while preserving operational covariance.
6. Stage 10F shows that wrong typing, wrong normalization, and weight/class misalignment do not obtain the same licensed result merely from retained numerical payloads.

Thus the strongest bounded conclusion is:

`fully typed finite future-measurement covariance = established`.

## What Stage 10 adds to the temporal candidate

The retained finite-model candidate remains:

`T10_candidate=(O,P,R,V;Xi)`

with:

`R=(R_content,R_direction,R_access)`

and:

`V=(V_extension,V_semantics,V_weights)`.

Stage 10 strengthens the role of `Xi`: event, continuation-class, outcome, normalization, and weight-alignment typing are not dispensable merely because some associated matrices or numbers can be reconstructed.

The key Stage 10F guard remains:

`numerical reconstructibility != typed operational identification`.

This does **not** imply:

`Xi typing resource == metaphysically fundamental entity`.

It establishes only that these resources perform non-redundant formal work in the declared operational comparison.

## Modal and blockness/becoming boundary

Stage 10 closes an operational covariance question, not the metaphysical dispute.

In particular:

- perspective-invariant future probabilities do not make a future event actual;
- matched epistemic and ontic-extension public views do not collapse their privileged modal structures;
- hidden `h*` swap invariance does not make a selected continuation observable;
- evidence-update covariance is not ontological becoming;
- successful covariance is not proof of eternalism;
- successful covariance is not a refutation of becoming.

Accordingly:

`future-measurement covariance != future actuality`.

`measurement covariance != modal/ontological identity`.

`perspective-invariant future probabilities != proof of eternalism`.

`measurement covariance != refutation of ontological becoming`.

## Remaining structural boundaries

The Stage 9 measurement-family gap is now removed from the unresolved list. The strongest remaining boundaries are:

- `finite typed clock measurement covariance != general covariance`;
- the `O` layer is still a deliberately minimal `e0<e1<e2` skeleton;
- the clock family remains finite and ideal/projective rather than nonideal/POVM;
- a direct `Xi_RV` value law remains `not_established`;
- operational covariance still does not decide modal ontology;
- no empirical discriminator between selected-history and selector-free extension semantics has been established.

## Stage 11 gate ranking

The executable Stage 10G ranking is:

1. `parametrized_covariance_precursor` — **9**
2. `richer_causal_order` — **7**
3. `nonideal_povm_clocks` — **6**

The completed `full_measurement_covariance` gate is not retained as a future candidate.

## Selected Stage 11 gate

> **Construct a parametrized covariance precursor that preserves the typed O/P/R/V measurement architecture without assuming a preferred external time parameterization.**

Why this gate is now selected:

- Stage 10 closes the known finite measurement-covariance boundary.
- The current finite O/P/R/V architecture is mature enough to carry a richer covariance test without simultaneously repairing a missing measurement layer.
- Stage 10F identifies which semantic correspondence resources must be transported rather than silently discarded.
- The project has repeatedly retained the guard `finite clock covariance != general covariance`; this is now the sharpest project-wide boundary left by the completed finite atlas.

The next stage should still be a **precursor**, not an immediate GR/quantum-gravity model. The controlled question is whether the current typed structure survives a parametrized constrained description in which an external evolution parameter is gauge/bookkeeping rather than physical time.

`parametrized covariance precursor != general relativity`.

## Why richer causal order is second

`richer_causal_order` remains important because the current O layer is schematic. It rises from the Stage 9G score of 6 to 7 after the measurement gap is closed.

It is nevertheless ranked second because a parametrized covariance precursor directly tests the repeated finite-clock/general-covariance boundary while changing one major structural assumption at a time. Richer causal order remains a strong follow-up or fallback gate if the parametrized construction proves too underdetermined.

## Why nonideal/POVM clocks are third

Stage 10 makes the nonideal-clock question better posed, especially because normalization cannot be reset to a fresh numerical identity under the current non-Euclidean-unitary maps. The tomography-complete probe family also supplies a strong future diagnostic.

However, this remains a narrower robustness perturbation of `P` than the selected parametrization/gauge boundary.

## Criteria 48–49

48. Executable synthesis selects among the frozen Stage 10 outcomes — **satisfied** with `measurement_covariant`.

49. The next gate is evidence-selected from remaining unresolved boundaries — **satisfied** with `parametrized_covariance_precursor`.

## Criterion 50

Criterion 50 is intentionally **not** closed by the synthesis code.

It still requires:

- a final documentation-synchronized full-repository regression;
- PR #11 merge-readiness review;
- confirmation that no unresolved review blocker remains.

## Interpretation guards

- `measurement_covariant finite family != general covariance`;
- `future-measurement covariance != future actuality`;
- `perspective-invariant future probabilities != proof of eternalism`;
- `measurement covariance != modal/ontological identity`;
- `measurement covariance != refutation of ontological becoming`;
- `numerical reconstructibility != typed operational identification`;
- `typed-resource necessity != metaphysical fundamentality`;
- `parametrized covariance precursor != general relativity`;
- `finite-model success != empirical discovery`.
