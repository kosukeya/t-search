# Stage 11F Notes — Ablation / Wrong-Gauge / False-Positive Controls

Stage 11F closes criteria **44–47** by pressure-testing the positive Stage 11A–E construction rather than adding a new physical model.

Source/unit-test checkpoint: run #1425 — **`924 passed in 586.95s (0:09:46)`**.

Central distinction:

`numerical reconstructibility != typed operational identification`.

Additional guards:

- `reconstructible != universally redundant`;
- `lost != metaphysically irreducible`;
- `missing typing != metaphysical absence`;
- `wrong-gauge failure != ontological becoming`;
- `finite-model ablation != fundamental ontology`;
- `not_established != false`.

## 1. Parameter-event correspondence ablation

The cubic Stage 11C architecture is copied with `Xi.event_correspondence` and its explicit semantics removed while O is left unchanged.

O still contains both typed role rows:

- `e1` / prediction anchor / Stage 11 physical event id;
- `e2` / measurement target / Stage 11 physical event id.

Therefore the correspondence can be reconstructed numerically from O. This does **not** restore the declared Xi typing resource. Stage 11C/11D validation rejects the ablated architecture as a well-typed cross-representation context.

Classification:

`event_correspondence_reconstructible_but_typed_identity_lost`.

Observed result:

- numerical payload: `reconstructible`;
- typed identification: `lost`;
- covariance claim: `not_established`;
- unchanged relational-q residual: **0.0**.

This is the direct Stage 11 analogue of the Stage 10F distinction between stored/reconstructible numerical forms and typed operational identity.

## 2. Missing lapse/Jacobian semantics

A second cubic architecture retains its correct numerical anchor/target lapse values but removes the Xi sentence specifying how lapse transforms under reparameterization.

The underlying Stage 11A/B trajectory is unchanged, so the stored numerical values still reconstruct

`dq/dT=(dq/dlambda)/(dT/dlambda)=1.25`.

The numerical residual against `p` is **0.0**, while Stage 11D context validation rejects the typed claim because the lapse/Jacobian transformation semantics are absent.

Classification:

`lapse_semantics_missing_typed_claim_not_established`.

This case remains distinct from an actually wrong Jacobian/lapse value.

## 3. Wrong lapse/Jacobian value

The cubic parameterization is deliberately assigned the identity-chart target lapse.

At the target source label `lambda=1`:

- identity/seed lapse = `1.25`;
- cubic Jacobian = `1.75`;
- correct cubic lapse = approximately `0.7142857142857143`;
- wrong reused identity lapse = `1.25`.

The resulting lapse-value residual and wrong-`dq/dT` residual are both approximately **0.5357142857142857**.

This supplies two independent witnesses:

1. Xi/context validation rejects the target lapse as inconsistent with the cubic parameterization;
2. using that wrong lapse changes `dq/dT` away from canonical momentum `p=1.25`.

Classification:

`wrong_lapse_jacobian_numerically_refuted`.

Thus missing semantics is `not_established`, while a wrong numerical lapse with an explicit derivative witness is `refuted` for the tested construction.

`not_established != refuted`.

## 4. Consolidated false-positive family

Stage 11F consolidates seven controls, all **7 / 7** rejected:

1. orientation reversal `f_rev(lambda)=-lambda` — **12** decreasing sampled steps;
2. non-injective square relabeling `f_noninj(lambda)=lambda^2` — **6** explicit collisions on the both-sign domain;
3. raw-equal-parameter event matching — **6** false physical-event identities;
4. parameter-dependent O corruption;
5. parameter-dependent P corruption;
6. parameter-dependent R corruption;
7. parameter-dependent V corruption.

The orientation-reversal control exhibits decreasing raw labels on the sampled source sequence rather than being rejected only by a metadata flag.

The square-map control exhibits explicit collisions between distinct signed source labels.

The raw-lambda control reuses the Stage 11B identity/affine witness and retains classification

`invalid_equal_raw_parameter_event_rule`.

The O/P/R/V controls reuse the Stage 11C validator; all **4 / 4** retain classification

`parameter_dependent_oprv_corruption_detected`.

## 5. Criterion mapping

- **44** — removal of parameter-event correspondence classified separately from numerical reconstructibility — **satisfied**;
- **45** — missing and wrong lapse/Jacobian semantics separated with explicit witnesses — **satisfied**;
- **46** — reversal, noninjective relabeling, raw-lambda matching, and parameter-dependent O/P/R/V corruption have explicit false-positive controls — **satisfied**;
- **47** — no ablation/control result promoted to metaphysical fundamentality or ontological becoming — **satisfied**.

Bounded result:

`Stage 11F typed-resource ablation and wrong-gauge false-positive controls = established on the frozen finite family`.

## 6. Interpretation boundary

`typed-resource necessity in this finite family != metaphysical fundamentality`.

`wrong-gauge failure != ontological becoming`.

`reconstructible resource != universally redundant resource`.

`finite-model ablation != theorem about reality in general`.

`repository validation != new scientific evidence`.

Next checkpoint: **Stage 11G — synthesis and evidence-selected next gate.**
