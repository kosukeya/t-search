# Stage 11G Notes — Synthesis and Evidence-Selected Next Gate

Stage 11G closes criteria 48–49 by integrating the executable Stage 11A–F evidence chain into one frozen Stage 11 status and one evidence-selected next gate.

Source/unit-test checkpoint: run #1453 — **`937 passed in 851.19s (0:14:11)`**.

## 1. Frozen Stage 11 status

The executable synthesis selects exactly one frozen status:

`parametrized_covariant`.

This label is deliberately bounded. It means that, on the declared finite positive family,

- four admissible external parameterizations represent the same sampled constraint orbit;
- corresponding physical events are identified by typed event correspondence rather than equal raw parameter labels;
- `q(T=tau)` and `dq/dT` are preserved across the positive family;
- the typed `T=(O,P,R,V;Xi)` architecture is preserved while representation metadata remain in Xi;
- the frozen Stage 10 future-measurement family retains per-continuation probability, weighting, modal public-view, and evidence-update covariance;
- external reparameterization `G` is compatible with the genuine internal-clock transport `C` on the tested product square family;
- wrong-gauge, missing-typing, noninjective, reversal, raw-lambda, and O/P/R/V corruption controls are classified/rejected as intended.

The synthesis does not use deliberate wrong controls as evidence of a positive-family obstruction. In particular, Stage 11F's wrong-lapse case is expected to be `refuted` because the control deliberately violates the declared transformation law.

## 2. Why `parametrized_covariant` rather than `parametrized_partial`

Stage 11A/B alone could still be read as a re-labeling result on one constraint orbit. Stage 11C/D carry the full typed O/P/R/V and future-measurement architecture across external parameterizations, Stage 11E tests compatibility with internal-clock changes, and Stage 11F distinguishes reconstructible payloads from licensed typed covariance claims.

Therefore all frozen Stage 11A–F criteria jointly close on the positive family. Within the predeclared status vocabulary, `parametrized_covariant` is the strongest warranted bounded label.

This does not license promotion to general covariance.

## 3. Updated finite candidate

The Stage 11 synthesis retains

`T11_candidate=(O,P,R,V;Xi)`

and adds the bounded structural fact that the finite candidate is equipped with:

- a typed external reparameterization atlas `G`;
- a continuation-aware internal-clock atlas `C`;
- tested compatibility `C o G ~= G o C` on the frozen finite product family.

`external parameterization atlas != fundamental spacetime gauge structure`.

## 4. Strongest unresolved boundary

Stage 11 still tests **four representations of one frozen sampled constraint orbit**. It does not yet establish a distinction between:

1. points/representations related by constraint-generated gauge redundancy; and
2. genuinely different physical constraint orbits arising from different initial data or conserved relational content.

Nor has the project yet implemented:

- a nontrivial constraint algebra;
- refoliation structure;
- diffeomorphism invariance;
- a dynamical metric;
- gravitational clock degrees of freedom.

Thus:

`one-orbit covariance != multi-orbit gauge covariance`.

`external parameterization independence != diffeomorphism invariance`.

`finite typed parametrized covariance != general covariance`.

## 5. Stage 12 candidate ranking

The executable ranking is:

| rank | gate | score |
| --- | --- | ---: |
| 1 | `multi_orbit_constraint_gauge_atlas` | **10** |
| 2 | `richer_causal_order` | **7** |
| 3 | `nonideal_povm_clocks` | **6** |
| 4 | `gravitational_minisuperspace_extension` | **5** |

The selected Stage 12 gate is:

> **Construct a multi-orbit constraint-generated gauge atlas that separates gauge-related parameterizations from physically distinct orbits and tests whether relational/Dirac observables and the typed O/P/R/V measurement architecture descend consistently across that atlas.**

This gate changes the strongest remaining assumption one step at a time. Stage 11 asks whether one orbit survives external reparameterization. Stage 12 should ask whether a family of constraint orbits admits a meaningful gauge/physical separation before any direct gravitational extension.

## 6. Why not jump directly to GR/minisuperspace?

A gravitational/minisuperspace extension remains a live candidate, but it is intentionally ranked below the gauge-atlas step. Moving directly to it would introduce several new ingredients simultaneously: multiple physical orbits, gauge-orbit identification, nontrivial constraints, possible refoliation structure, dynamical geometry, and gravitational clock semantics.

The selected Stage 12 gate instead tests the closest unresolved structural boundary without presupposing general covariance.

`constraint-generated gauge precursor != general relativity`.

## 7. Ontological boundary

Stage 11 does not establish:

- that future events are already actual;
- eternalism;
- a block universe as a metaphysical fact;
- absence of ontological becoming;
- modal/ontological identity of epistemic selected-history and selector-free ontic-extension semantics;
- metaphysical fundamentality of Xi or any retained typing resource.

Therefore:

`parameterization-covariant future probabilities != future actuality`.

`parameterization-covariant future probabilities != proof of eternalism`.

`absence of preferred external parameterization != absence of ontological becoming`.

`typed-resource necessity != metaphysical fundamentality`.

## 8. Criterion mapping

- criterion **48** — executable Stage 11 synthesis selects exactly one frozen status from Stage 11A–F evidence — **satisfied**;
- criterion **49** — next research gate is evidence-selected without presupposing general covariance — **satisfied**;
- criterion **50** — external final full-repository regression / merge-readiness review — **pending**.

`repository validation != new scientific evidence`.

Next checkpoint: **criterion 50 external final repository validation / merge-readiness review.**