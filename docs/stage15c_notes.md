# Stage 15C Notes — Dirac / Complete Relational Observables and Physical Quotient

Status: **validated Stage 15C scientific checkpoint; criteria 25–31 satisfied. Stage 15D is next.**

Validated Stage 15C scientific checkpoint: `8d2952585c106c6f56843657aa28b30e67fbd077`, GitHub Actions run #1962, **`1203 passed in 618.87s (0:10:18)`**.

## Question tested

Stage 15C asks whether the Stage 15A/15B finite carrier and compensated path families descend to representative-independent Dirac data, a four-class sampled physical quotient, and nontrivial complete relational observables.

The frozen Dirac pair is

`P_D=P`,

`Q_D=Q-c_0 T_0-c_1 T_1-c_2 T_2`.

The complete three-condition relational observable is

`Q(T_0=tau_0,T_1=tau_1,T_2=tau_2)=Q_D+c_0 tau_0+c_1 tau_1+c_2 tau_2`.

## Strong Dirac commutation

The raw Dirac pair is reconstructed from every one of the **108** positive representatives and checked against all three presented constraints.

The same Poisson-commutation audit is repeated on **108** frozen off-surface probes, for **216** tested phase-space points total. This prevents a merely weak-on-surface Dirac claim.

## Sampled quotient and orbit discrimination

The reconstructed quotient contains exactly **4 classes x 27 representatives**.

All **6/6** unordered pairs among the four declared physical orbits are separated by the full `(Q_D,P_D)` pair, with minimum sampled max-norm separation **0.5**.

Anti-triviality controls include one same-`P_D`/different-`Q_D` pair and one same-`Q_D`/different-`P_D` pair, so neither invariant alone is promoted to the full quotient label.

Stage 15C additionally tests sampled within-class reachability rather than grouping by invariant label alone. The canonical `C_0 -> C_1 -> C_2` local-flow construction is checked for all

`4 x 27 x 27 = 2916`

same-orbit ordered source/target pairs. All **2916/2916** sampled pairs are reachable within tolerance. Cross-orbit path construction remains rejected; **8748** ordered cross-orbit representative pairs are rejected.

`sampled same-orbit reachability != universal gauge-orbit theorem`.

## Complete relational family

Across 108 source representatives and 27 target clock triples, Stage 15C evaluates **2916** complete relational values.

The within-orbit relational span is **3.5** for each of the four sampled physical classes. Thus representative-independent Dirac data coexist with nontrivial relational change on the frozen finite carrier.

`Dirac-invariant data + relational change != proof of eternalism`.

## Compensated-path descent

The complete observable is retested across both Stage 15B path families:

- local compensated relational comparisons: **23328**;
- constant-smeared predicted-compensation relational comparisons: **14580**.

The compared path orders descend to the same complete-relational target within the declared tolerance.

`compensated relational descent != refoliation invariance`.

## Incomplete-observable controls

All three one-condition-omitted variants are tested rather than selecting only one clock direction.

- omitted-clock evaluations: **324**;
- fixed two-condition groups: **108**;
- groups retaining raw gauge dependence: **108/108**.

Residual spreads are:

- omit `T_0`: **2.0**;
- omit `T_1`: **1.0**;
- omit `T_2`: **0.5**.

The raw representative coordinate `Q` is also rejected as a quotient observable: it varies with span **3.5** inside every sampled physical class.

Classification for the omitted-clock family: `relational_observable_incomplete`.

## Criteria 25–31

Stage 15C closes only criteria **25–31**:

25. raw Dirac reconstruction and positive/off-surface strong commutation;
26. exact sampled `4 x 27` quotient plus **2916/2916** within-class reachability;
27. `6/6` full-Dirac-pair orbit discrimination;
28. **2916** complete-relational evaluations with nontrivial relational change;
29. **23328** local and **14580** smeared compensated relational descent comparisons;
30. all three omitted-clock controls plus raw-`Q` non-descent;
31. bounded quotient/relational interpretation without importing Stage 15D locality claims.

Criteria **32–50 remain pending at the Stage 15C checkpoint**.

## Bounded result

`Stage 15C representative-independent Dirac / complete-relational / sampled four-class quotient descent = established`.

This does not establish a universal reduced phase space, refoliation invariance, a continuum hypersurface-deformation algebra, general relativity, locality-protected non-Abelianity, eternalism, future actuality, or ontological becoming.

Guards:

- `sampled four-class quotient != universal reduced-phase-space theorem`;
- `sampled same-orbit reachability != universal gauge-orbit theorem`;
- `complete relational observable != ontological becoming by definition`;
- `Dirac-invariant data + relational change != proof of eternalism`;
- `compensated relational descent != refoliation invariance`;
- `Stage 15C quotient descent != Stage 15D locality-protected non-Abelianity`;
- `repository validation != new scientific evidence`.
