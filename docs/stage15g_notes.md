# Stage 15G — Executable synthesis and evidence-selected Stage 16 gate

Status: **validated; criteria 48–49 satisfied. Criterion 50 remains external and pending.**

## Validation checkpoint

Stage 15G scientific selector logic last changed at:

`dca5dfb1c0faf3f7e57b07664529166f0f12b12d`

The Stage 15G source logic, Stage 15A–F evidence chain, and subsequent top-level historical-compatibility corrections are jointly validated at branch head:

`de43fb44f4c0e6fc7095afef34805e7a1ba96b8b`

GitHub Actions:

- push run #1995: **`1255 passed in 886.65s (0:14:46)`**;
- PR run #1996: **`1255 passed in 553.80s (0:09:13)`**.

The earlier PR run #1988 also validated the corrected Stage 15G selector implementation with **`1255 passed in 905.93s (0:15:05)`**. Subsequent #1992–#1994 failures were documentation-compatibility regressions caused by top-level roadmap wording/hash loss; they did not change or refute the Stage 15G scientific source. Runs #1995/#1996 validate the repaired current head.

`repository validation != new scientific evidence`.

## Criterion 48 — frozen synthesis selection

The executable selector consumes only the validated Stage 15A–F evidence and selects exactly the frozen Stage 15G vocabulary value:

`spatial_local_path_covariant_local_abelianizable`.

The selection is supported by the following bounded chain:

- Stage 15A: a three-site spatially indexed local/smeared first-class structure-function carrier with 108 representatives in four physical classes;
- Stage 15B: exact compensated closure for 864 local pairs and 540 smeared ordering probes, with off-surface Jacobi and independent Hamiltonian-flow checks;
- Stage 15C: four 27-member quotient classes, all-pairs sampled within-class reachability, full Dirac-pair orbit discrimination, and complete three-clock relational descent;
- Stage 15D: an admissible one-step L1 nearest-neighbour shear `C1 -> C1-kappa*T1*C2=K1` yields a strongly commuting basis while preserving tested physical content; the full seed reconstruction remains non-one-step-L1 and factors at exact Lfinite depth 2;
- Stage 15E: typed O/P/R/V/Xi public/future payloads descend across representatives, local/smeared paths, independently rebuilt smeared endpoints, and all Stage 15D basis candidates;
- Stage 15F: all 15/15 frozen destructive/anomaly/false-positive controls are rejected in their intended layers.

Therefore the declared one-step L1 locality class does **not** protect the open three-site carrier's noncommuting presentation from Abelianization.

This is an existential result on the frozen carrier. It is not an exhaustive classification of all locality-preserving bases and it does not establish universal local Abelianizability.

`one-step L1 Abelianization on an open three-site chain != universal local Abelianizability`.

`local Abelianization != absence of meaningful local constraint structure`.

## Project-question synthesis

The executable synthesis retains eight explicit project questions. Their bounded answers are:

1. a nontrivial finite spatially indexed local/smeared first-class structure-function precursor is established;
2. local and smeared ordered paths close under the algebraically predicted compensation on the tested family;
3. Dirac data, the sampled physical quotient, and complete relational observables descend while relational change remains nontrivial;
4. the frozen one-step L1 locality class does not protect noncommutativity on the open three-site carrier;
5. typed O/P/R/V/Xi future-measurement content descends across representative, path, and equivalent-basis choices on the frozen family;
6. the frozen destructive controls reject their intended false positives;
7. Stage 15 does not establish GR, a hypersurface-deformation algebra, refoliation invariance, relativistic locality, fundamental non-Abelianity, eternalism, ontological becoming, or future actuality;
8. the strongest next structural pressure test is to remove the open-chain terminal-seed / triangular-elimination loophole while preserving nontrivial one-step locality.

`Dirac-invariant data + relational change != proof of eternalism`.

`complete relational observable != ontological becoming by definition`.

`future-measurement covariance != future actuality`.

## Criterion 49 — evidence-selected Stage 16 ranking

The executable ranking is:

1. `four_site_closed_cycle_constraint_algebra_precursor` — **15**;
2. `larger_sparse_graph_locality_scaling_audit` — **11**;
3. `admissible_basis_transformation_completeness_audit` — **9**;
4. `gravitational_minisuperspace_extension` — **8**;
5. `nonideal_povm_clock_extension` — **6**.

Selected Stage 16 gate:

`four_site_closed_cycle_constraint_algebra_precursor`.

> **Construct a minimal four-site closed-cycle spatially indexed first-class constraint-algebra precursor with no terminal seed generator, retain explicit local/smeared structure-function dependence, test whether one-step L1 or finite-depth locality-preserving Abelianization still exists, and retest compensated paths, the physical quotient, complete relational observables, and typed O/P/R/V/Xi descent without assuming general relativity or refoliation invariance.**

### Why a four-site cycle

The positive Stage 15 L1 witness peels the terminal `C2=K2` seed of an acyclic open chain. A closed cycle removes that terminal tail and directly pressures the structural feature most plausibly responsible for the Stage 15 Abelianization.

A three-site cycle is insufficient as a locality pressure test because every radius-1 neighbourhood contains all three sites, so L1 locality would become effectively global. A four-site cycle is the smallest closed graph for which `N1(i)` still excludes one site and therefore remains a genuine restriction.

This gate is selected because it changes one structural ingredient at a time before importing gravitational assumptions. It is **not** selected because a locality obstruction is predicted.

`closed-cycle selection != predicted locality obstruction`.

## Unresolved boundaries carried forward

- open-three-site one-step L1 Abelianization does not imply universal local Abelianizability;
- terminal-seed tail peeling does not determine the closed-cycle or branched-graph result;
- finite graph locality does not establish continuum or relativistic locality;
- finite smeared constraint algebra does not establish a hypersurface-deformation algebra;
- compensated local/smeared paths do not establish refoliation invariance;
- the sampled four-class quotient is not a continuum reduced-phase-space theorem;
- typed O/P/R/V/Xi descent is not an independently derived empirical measurement law;
- the orbit-sensitive future witness is not an empirical prediction;
- Dirac-invariant data plus relational change decides neither eternalism nor ontological becoming;
- future-measurement covariance does not imply future actuality;
- destructive-control rejection demonstrates validator discrimination, not continuum correctness;
- finite-model success is not empirical discovery.

## Criteria state

- criteria **1–47**: satisfied by Stage 15.0–F;
- criterion **48**: satisfied by the executable frozen-vocabulary synthesis selection;
- criterion **49**: satisfied by the executable evidence-selected Stage 16 ranking and gate;
- criterion **50**: **pending external final full-repository regression / merge-readiness review**.

Stage 16 has not started.
