# Stage 7 Protocol — Quantum Records in a Constrained Multi-Clock Model

Status: **Stage 7.0 protocol frozen; Stage 7A–7F completed; Stage 7G is next.**

Stage 7 follows the Stage 6G evidence-selected gate. The earlier roadmap chronology is **superseded by the Stage 6G gate selection**: before attempting a generally covariant / gravitational extension, place an explicit memory degree of freedom and record semantics inside the constrained multi-clock quantum construction and pressure-test them under genuine clock changes.

The Stage 6 candidate entering Stage 7 is:

`T6_candidate=(O,P,R,V;Xi)`.

Stage 7 primarily pressure-tests `P`, `O`, and `R`. It does not yet integrate `V` into the same quantum construction.

## 1. Central question

> In one constrained finite quantum model containing multiple admissible internal clock perspectives and an explicit memory degree of freedom, does record-defined temporal structure remain distinct from perspective transformation and neutral order while transforming consistently under genuine clock changes?

A positive result strengthens the Stage 6 layered candidate by removing the former product-model separation between Stage 5 `P` and Stage 3/6E `R`. A negative result must revise that candidate if record structure reduces to retained `P/O/Xi`, fails covariance, or destroys the perspective structure required for the comparison.

## 2. Scope and non-goals

Stage 7 is a finite constrained-quantum pressure test. It does not establish a fundamental ontology of time, ontological becoming, phenomenal passage, a thermodynamic arrow, universal quantum-reference-frame availability, general covariance of gravity, realistic time dilation, or a new empirical prediction.

The former roadmap item assigning Stage 7 directly to a generally covariant / gravitational extension is deferred rather than discarded.

## 3. Carrier and spectator baseline

The canonical carrier is

`H_kin^7 = H_A tensor H_B tensor H_C tensor H_M`,

with `H_M=C^2`.

For Stage 7A spectator memory:

`H_M^(0)=0`,

`H_tot^(0)=H_tot^(5) tensor I_M`.

The spectator model is a strict no-record control.

`memory present != record present`.

Stage 7A verified the 54-dimensional kinematic carrier, 14-dimensional physical space, 14-dimensional per-clock supports inside 18-dimensional reduced ambient spaces, reduction/reconstruction round trips, 54 distinct-clock comparisons, 162 three-clock composition cases, and zero target-memory record information in the spectator control.

## 4. Record semantics

A Stage 7 record claim requires:

1. explicit target variable/operator;
2. explicit memory readout;
3. target-specific information diagnostic;
4. no-record / wrong-target controls where applicable;
5. explicit event/history correspondence before directional interpretation.

Frozen distinctions:

- `memory present != record present`;
- `entanglement != record`;
- `mutual information != directional record by itself`;
- `record correlation != record-defined temporal orientation`.

Stage 7B uses `Q=[B energy label == -1]`, wrong target `W=[C energy label == +1]`, computational memory readout, and reversible controlled write

`U_rec = Q tensor X_M + (I-Q) tensor I_M`.

It raises `I(Q;M)` from `0` to `1 bit`, leaves the wrong-target information at zero, is reversible, and lifts to a physical-subspace automorphism. No directional score is inferred from Stage 7B alone.

## 5. Relational record formation

Stage 7C embeds an internal history

`e0 < e1 < e2`

into a modified constraint through

`W_hist = sum_j |t_j><t_j|_A tensor V_j`,

`H_hist = W_hist H_tot^(0) W_hist^dagger`.

Forward schedule:

- `V_0=I`;
- `V_1=U_rec`;
- `V_2=U_scr U_rec`.

At current event `e1`:

`A_R = I(M_e1;Q_e0) - I(M_e1;Q_e2)`,

`A_acc = Acc(Q_e0|M_e1) - Acc(Q_e2|M_e1)`.

Stage 7C gives:

- forward: `A_R=+1`, `A_acc=+1/2`, `lower-index`;
- explicit reversed history: `A_R=-1`, `A_acc=-1/2`, `upper-index`;
- balanced forward/reverse meta-ensemble: signed scores cancel;
- no-record history: signed scores cancel;
- maximally mixed-memory control: signed scores cancel.

Guards:

- `simulation/intervention order != modeled temporal order`;
- `physical-subspace automorphism != time-localized dynamical interaction`;
- `clock-conditioned conjugated constraint != unique autonomous interaction Hamiltonian`;
- `record-defined orientation != thermodynamic arrow / ontological becoming / phenomenal passage`.

## 6. Perspective and event typing

Stage 7 keeps distinct:

- horizontal perspective transformations `S_{q<-p}`;
- vertical modeled history/order relations;
- explicit event correspondence `chi_{q<-p}`;
- target and memory observables.

Equal numeric clock readings do not identify events across perspectives.

`P-R covariance != P=R`.

## 7. Stage 7D — genuine clock-change record transport — completed

The Stage 7C modified physical space is reduced at all nine clock/readout nodes `(X,j)` for `X in {A,B,C}` and `j in {0,1,2}`. All reductions retain rank 14.

Writing one reduction in image coordinates as `y_X=C_X c`, define

`G_X=C_X^{-dagger} C_X^{-1}`

and

`S^hist_{Y<-X}=C_Y C_X^{-1}`.

All 54 directed distinct-clock/readout comparisons satisfy state transport, inverse consistency, corresponding-observable transport, and

`S^dagger G_Y S = G_X`

within tolerance. The interaction therefore deforms the ideal Euclidean-unitary Stage 5/7A atlas into a nonideal induced-metric-preserving atlas rather than destroying the perspective structure.

Preserving `chi` yields `A_R=+1`, `A_acc=+1/2` at all nine nodes. Reversing `chi` yields `A_R=-1`, `A_acc=-1/2`.

Negative controls reject the inherited spectator map, a same-bare-observable comparison, and a wrong/misdeclared `chi`.

## 8. Stage 7E — accessibility and partial-atlas consistency — completed

Stage 7E changes only the declared local memory-readout interface while holding the common physical record state and record operators fixed.

- `full`: local `A_R=+1`, `A_acc=+1/2`;
- `hidden`: global record remains represented, local `A_R=0`, `A_acc=0`;
- `maximally-noisy`: global record remains represented, local `A_R=0`, `A_acc=0`;
- `coarse`: local `A_R=1-H_2(1/4) ~= 0.1887218755`, local `A_acc=1/4`.

Therefore:

`locally inaccessible record != globally absent record`.

For the partial atlas the primitive edge `A/e1 -> B/e0` is withheld while three indirect paths through `C/e0`, `C/e1`, and `C/e2` reproduce the omitted direct-map oracle, state, induced metric, corresponding record observables, `A_R=+1`, and `A_acc=+1/2` within tolerance.

Perturbing only `C/e1 -> B/e0` localizes map/state/metric/record-statistic inconsistency while the other two paths remain consistent. The tested projector algebra can still similarity-transport.

Guard:

`observable-algebra correspondence != full state/metric path consistency`.

## 9. Stage 7F — ablation / reconstruction / mismatch matrix — completed

Stage 7F freezes six represented roles:

- `target_specific_record`;
- `record_defined_direction`;
- `local_record_readout`;
- `perspective_transport`;
- `P_R_covariance`;
- `internal_history_anchor`.

Statuses are:

- `preserved`;
- `reconstructible`;
- `inaccessible`;
- `lost`;
- `not_established`.

`lost` is a statement about the declared ablation interface, not metaphysical irreducibility. `reconstructible` does not mean universal redundancy.

### 9.1 Memory removal

Removing the memory variable from the retained record carrier collapses both lower and upper target-memory information to zero:

- `A_R=0`;
- `A_acc=0`;
- orientation `none`.

The A/B/C clock carrier remains available. Record loss is therefore not identified with perspective loss.

### 9.2 Record-coupling neutralization

The internally anchored Stage 7C no-record family retains:

- `e0<e1<e2` anchoring;
- rank-14 reductions;
- 54 distinct-clock comparisons;
- state transport;
- inverse consistency;
- induced-metric covariance,

all within `1e-9` in the Stage 7F regression.

Yet:

- `record_defined=false`;
- `A_R=0`;
- `A_acc=0`.

Therefore the implication

`P + internal O => R`

is refuted in the declared Stage 7 family.

This is a finite-model counterexample, not a theorem that P/O can never generate records in another model.

### 9.3 History-anchor removal

Falling back to the Stage 7B witness preserves `I(Q;M)=1 bit` and the target-specific record correlation, but `directional_score_defined=false`.

Therefore target-specific correlation is preserved while record-defined direction is `not_established` and the internal history-anchor role is lost.

### 9.4 Explicit perspective-edge removal

With the explicit cross-clock edge matrices withheld but the common physical carrier and per-node reductions retained, all 54 maps are reconstructed as

`S_{Y<-X}=C_Y @ inv(C_X)`.

Reference-map agreement, state transport, inverse consistency, metric covariance, record-score covariance, and accessibility covariance remain within `1e-9`.

Thus the explicit edge-matrix representation is `reconstructible`.

Guard:

`explicit perspective-map reconstruction != elimination of the perspective layer`.

### 9.5 Event correspondence removal

With local P and R retained but `chi` withheld, single-perspective record orientation remains represented while cross-perspective `P_R_covariance` is `not_established`, not false.

### 9.6 Access neutralization

Hidden and maximally noisy interfaces preserve global R and global P-R covariance while local record readout is `inaccessible`.

### 9.7 Mismatch controls

Wrong/misdeclared `chi` gives record-score residual `2` and accessibility residual `1`.

The perturbed `C/e1 -> B/e0` edge remains a localized path mismatch with nonzero map/state/metric/record-score residuals while unaffected paths remain consistent.

## 10. Stage 7F structural consequence

The strongest bounded result before Stage 7G is:

> Within the declared Stage 7 finite constrained family, the record-defined role R is not reconstructed from the retained tested perspective structure plus internal neutral event anchoring when record coupling is neutralized, whereas explicit cross-clock edge matrices are reconstructed from the common physical carrier and per-perspective reductions.

This strengthens the functional layered interpretation of P/O/R while refining what is primitive inside P. It does not establish metaphysical irreducibility of R or universal redundancy of P.

## 11. Evidence taxonomy

Every Stage 7 claim is one of:

1. **Executable witness**;
2. **Established finite-model result**;
3. **Candidate structural interpretation**;
4. **Untested / not established**.

Hand-written verdict booleans copied from prose are not executable evidence.

## 12. Stage 7 sequence

- **Stage 7.0 — protocol freeze — completed**
- **Stage 7A — spectator-memory constrained baseline — completed**
- **Stage 7B — reversible quantum record witness — completed**
- **Stage 7C — relational record formation and orientation controls — completed**
- **Stage 7D — genuine clock-change record transport — completed**
- **Stage 7E — accessibility and partial-atlas record consistency — completed**
- **Stage 7F — ablation / reconstruction / mismatch matrix — completed**
- **Stage 7G — synthesis and Stage 8 gate — next**

## 13. Candidate Stage 8 gates

Stage 7G must rank at least:

- integrate explicit `V` / extension semantics into the same relational quantum construction;
- move to richer causal/order structure;
- test interacting, nonideal, or POVM clocks;
- if the finite layered architecture is stable enough, begin a parametrized / generally covariant precursor.

## 14. Frozen interpretation guards

Stage 7 inherits prior guards and includes:

- `memory subsystem != conscious observer`;
- `memory present != record present`;
- `entanglement != record`;
- `target-specific record correlation != record-defined direction`;
- `record orientation != thermodynamic entropy arrow`;
- `record orientation != ontological becoming`;
- `record orientation != phenomenal passage`;
- `perspective change != temporal succession`;
- `P-R covariance != P=R`;
- `locally inaccessible record != globally absent record`;
- `indirect reconstructibility != direct local edge availability`;
- `localized path inconsistency != spacetime curvature`;
- `observable-algebra correspondence != full state/metric path consistency`;
- `lost != metaphysically irreducible`;
- `reconstructible != universally redundant`;
- `P + O retained without R != proof that P/O can never generate records in another model`;
- `missing chi != false covariance`;
- `wrong chi mismatch != destruction of P or R`;
- `explicit perspective-map reconstruction != elimination of the perspective layer`;
- `not_established != false`;
- `success with finite clocks != general covariance`;
- `Stage 7 synthesis != empirical discovery unless a new discriminating prediction is independently derived`.

## 15. Stop / revise conditions

Revise rather than force a positive conclusion if record is inferred only from entanglement; target/event correspondence is undeclared; Python execution order is used as physical temporal order; a modified constraint silently reuses old maps; local hiding is described as global destruction; `not_established` is converted to `false`; or an ablation is interpreted as metaphysical fundamentality.

## 16. Stage 7 exit criteria

### Protocol / typing — 1–5

Satisfied by Stage 7.0 and maintained through Stage 7F.

### Baseline — 6–9

Satisfied by Stage 7A.

### Record witness — 10–14

10–13 satisfied by Stage 7B; 14 by Stage 7C.

### Reversal / controls — 15–18

Satisfied through Stage 7C.

### Cross-perspective transport — 19–25

Satisfied by Stage 7D in the declared canonical interacting family.

### Accessibility / atlas — 26–29

Satisfied by Stage 7E in the declared canonical family.

### Minimality / synthesis — 30–36

30. memory / record / perspective / access / correspondence ingredients are ablated or neutralized one at a time where applicable — **satisfied by Stage 7F**;
31. `lost`, `reconstructible`, `inaccessible`, and `not_established` are distinguished — **satisfied by Stage 7F**;
32. Stage 7G explicitly decides whether the Stage 6 layered candidate is strengthened, reduced, broken, or inconclusive — pending;
33. unresolved implications remain `not_established` unless directly decided — pending final synthesis audit;
34. Stage 8 gate selected by discriminating power — pending;
35. interpretation guards preserved in synthesis — pending;
36. final full regression and PR merge-readiness review have no unresolved blocker — pending.

Thus Stage 7F closes criteria **30–31**. Stage 7G and final merge-readiness review remain.
