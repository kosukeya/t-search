# Stage 8 Protocol — Quantum Potentiality in the Shared Constrained Construction

Status: **Stage 8.0 protocol frozen; Stage 8A continuation substrate and Stage 8B typed modal models completed. Stage 8C is next.**

Selected Stage 8 gate:

> **Integrate explicit Potentiality / extension semantics `V` into the same constrained quantum construction.**

Current candidate: `T_candidate=(O,P,R,V;Xi)`.

## Central question

Can epistemic uncertainty about an already-selected quantum continuation and ontic representation of admissible quantum continuations with no selected complete future datum coexist on the same constrained carrier with matched operational structure while remaining formally distinct?

## Frozen guards

- `Potentiality != quantum randomness by definition`;
- `Potentiality != superposition by definition`;
- `Potentiality != Born probability by definition`;
- `random sampling != ontic actualization evidence`;
- `density matrix decomposition != unique modal semantics`;
- `operational quantum equivalence != modal/ontological equivalence`.

## Selected-vs-unselected model roles

Epistemic:

`M_E^Q=(QCarrier, D, h*, q_E)`.

One complete continuation `h*` is selected globally but hidden from the operational interface. The hidden selected continuation must not be consulted when computing the pre-discriminating operational prediction.

Ontic-extension:

`M_O^Q(D)=(QCarrier, D, QExt(D), K)`.

**No selected complete continuation datum may exist before update.** This includes hidden selectors, seed-equivalent selectors, precomputed outcomes, and latent branch variables serving the same role.

`no selected continuation field != proof of ontic openness in nature`.

## Genuine integration

A **physically admissible quantum continuation** must contain executable physical structure acting on the Stage 7 carrier; string labels or unrelated metadata are insufficient.

`typed modal wrapper beside quantum model != quantum-modal integration`.

## Stage 8A — common quantum-extension substrate — completed

`QExt(e1) = {h_L, h_R}` contains two physically inequivalent continuations sharing the same constrained Actuality through `e1` and current `I(Q;M)=1 bit`, then differing only at `e2` by a memory-neutral, record-target-neutral reversible C-sector phase.

Both continuation-specific constrained constructions retain physical dimension 14 and rank-14 reductions at all nine A/B/C clock/readout nodes. Renaming/current-prefix/terminal controls pass.

Stage 8A satisfies criteria 11–16. Final synchronized regression: `582 passed in 122.49s`.

## Stage 8B — typed epistemic and ontic-extension quantum models — completed

Stage 8B uses one `QuantumContinuationCarrier` with `{h_L,h_R}` and passes the exact **same carrier object** to both model types.

The epistemic model stores privileged `selected_continuation=h*` plus carrier and q_E.

The ontic model is frozen/slots and stores carrier plus K only. Its selector audit finds no selected/selector/seed/precomputed-outcome/latent-branch field, singleton `QuantumContinuation`, or dynamic instance `__dict__`.

Concrete Potentiality types:

- `EpistemicQuantumPotentiality`;
- `OnticExtensionQuantumPotentiality`.

Canonical matched weights:

`q_E = K = (0.5,0.5)`.

They are generated from the carrier without consulting `h*`.

With carrier and weights fixed, swapping epistemic `h*=h_L` to `h*=h_R` changes the privileged selected-continuation diagnostic while leaving the Stage 8B minimal pre-discriminating projection unchanged. That projection exposes neither `h*` nor model type.

Corrected implementation regression: `595 passed in 140.99s`.

Planning/documentation-synchronized regression: `597 passed in 84.13s`.

Stage 8B satisfies criteria 17–21.

Frozen guards:

- `same physical continuation carrier != same modal type`;
- `formal selected-vs-unselected difference != empirical physical difference`;
- `matched numerical q_E and K != matched probability semantics`;
- `hidden h* diagnostic != operational access to h*`;
- `Stage 8B pre-discriminating view != full Stage 8C O_Q interface`.

## Stage 8C target — operational underdetermination and explicit update

The full ontology-neutral interface is schematically:

`O_Q(G)=(A_Q(D),Next_Q(D),pi_Q(next|D),R_now,Obs_now)`.

It may expose declared current physical/record/next-outcome information but must not expose `h*`, model type, or privileged modal diagnostics.

Stage 8C must test:

- criterion 22: matched full operational views;
- criterion 23: privileged diagnostics still distinguish structures;
- criterion 24: weight mismatch can change operational predictions;
- criteria 25–27: explicit common-evidence update with epistemic h* retention/conditioning and ontic extension pruning without adding hidden complete future;
- criteria 28–29: superposition/Born randomness/state representation alone do not silently decide modal semantics.

## Stage 8D/E/F/G targets

Stage 8D: genuine P-V transport using interacting clock maps and explicit continuation/event correspondence.

Stage 8E: P/O/R/V compatibility and underdetermination.

Stage 8F: ablation/reconstruction/mismatch matrix.

Stage 8G: synthesis and evidence-selected next gate.

## Exit criteria status

- criteria 1–10: Stage 8.0 protocol/typing;
- criteria 11–16: Stage 8A completed;
- criteria 17–21: Stage 8B completed;
- Criteria 22–50 remain future scientific work.

## Sequence

- Stage 8.0 — Quantum Potentiality protocol freeze — completed
- Stage 8A — common quantum-extension substrate — completed
- Stage 8B — typed epistemic and ontic-extension quantum models — completed
- **Stage 8C — operational underdetermination and explicit update — next**
- Stage 8D — genuine clock-change modal transport
- Stage 8E — P/O/R/V compatibility and underdetermination
- Stage 8F — ablation / reconstruction / mismatch matrix
- Stage 8G — synthesis and evidence-selected next gate

## Strongest allowed Stage 8B statement

**Within the canonical Stage 8A constrained continuation carrier, two type-distinct modal models can be represented without changing the physical continuation substrate: an epistemic model stores privileged selected `h*`, whereas a frozen/slots ontic-extension model stores the shared carrier and weights but no selected continuation or selector-like datum in its declared schema. With matched `(0.5,0.5)` weights generated independently of `h*`, swapping `h_L` and `h_R` as selected continuation changes the privileged diagnostic while leaving the Stage 8B pre-discriminating public projection unchanged. This establishes a selected-versus-unselected typed model distinction, not full `O_Q` operational underdetermination, update semantics, P-V covariance, V independence from P/O/R, or an ontology of the real future.**
