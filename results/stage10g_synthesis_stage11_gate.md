# Stage 10G Results — Synthesis and Evidence-Selected Stage 11 Gate

Status: **criteria 48–49 satisfied; criterion 50 remains external.**

## Synthesis result

Executable choice:

`measurement_covariant`

Current bounded candidate:

`T10_candidate=(O,P,R,V;Xi)`

with retained internal typing:

- `R=(R_content,R_direction,R_access)`;
- `V=(V_extension,V_semantics,V_weights)`.

The Stage 10 result is explicitly scoped to the declared finite continuation-aware A/B/C future-signature measurement family.

## Evidence chain

| layer | Stage 10 result |
| --- | --- |
| reference measurement | Stage 9C outcomes/effects/likelihoods reproduced with explicit typing |
| continuation lift | continuation-specific support/physical lifts and operational normalization selected |
| representation transport | 18 charts, 108 genuine transports, 324 three-clock compositions |
| probability covariance | per-continuation Born/completeness/positivity covariance established |
| anti-accident control | 196 tomography-complete probes / 7056 probe outcome evaluations |
| weighted/modal/update layer | weighted predictions, matched public views, hidden-selector controls, and posteriors covariant |
| ablation layer | missing correspondence/normalization semantics distinguished from explicitly refuted wrong rules |

The integrated result therefore upgrades the Stage 9 status:

`full Stage 9C future-measurement covariance: not_established`

to the Stage 10 bounded status:

`fully typed finite future-measurement covariance: established`.

## Synthesis classification

The code selects `measurement_covariant` only when all of the following are jointly satisfied:

- valid typed Stage 10A reference family;
- valid continuation-specific Stage 10B lift/normalization decision;
- valid Stage 10C representation covariance and correspondence controls;
- Stage 10D `measurement_covariance_status == established`;
- Stage 10E `weighted_modal_update_covariance_established == True`;
- Stage 10F correspondence/normalization classifications and all required false-positive controls.

This prevents the synthesis from being driven by one positive numerical equality alone.

## Typed-resource conclusion

Stage 10F establishes a distinction that is retained in the synthesis:

`numerical reconstructibility != typed operational identification`.

Event/class/outcome correspondence, normalization semantics, and continuation-weight alignment are retained as formal resources needed to license the claim that two charts ask the same operational question.

Their role is **not** promoted to a claim of metaphysical primitiveness.

## Ontological boundary

The Stage 10 synthesis does not establish:

- that future events are already actual;
- that epistemic selected-history and selector-free ontic-extension semantics are identical;
- eternalism;
- ontological becoming;
- absence of ontological becoming;
- a fundamental or unique temporal ontology.

Thus:

`future-measurement covariance != future actuality`.

`measurement covariance != modal/ontological identity`.

`measurement covariance != proof of eternalism`.

`measurement covariance != refutation of ontological becoming`.

## Remaining gate ranking

| rank | gate | score |
| --- | --- | ---: |
| 1 | `parametrized_covariance_precursor` | **9** |
| 2 | `richer_causal_order` | **7** |
| 3 | `nonideal_povm_clocks` | **6** |

The completed `full_measurement_covariance` gate is removed from future candidates.

## Selected Stage 11 gate

> **Construct a parametrized covariance precursor that preserves the typed O/P/R/V measurement architecture without assuming a preferred external time parameterization.**

The selected gate is deliberately narrower than a direct gravitational extension. Stage 11 should first introduce a parametrized constrained model and test whether the current physical/clock/record/modal/measurement structures are invariant or reconstructible under reparameterization/gauge changes.

This directly addresses the repeatedly retained boundary:

`finite clock covariance != general covariance`.

while preserving the discipline of changing one major unresolved ingredient at a time.

## Criterion closure

- criterion **48** — executable synthesis choice — **satisfied**;
- criterion **49** — evidence-selected Stage 11 gate — **satisfied**;
- criterion **50** — final full-repository regression / merge-readiness review — **not yet closed**.

## Guards

- `measurement_covariant finite family != general covariance`;
- `future-measurement covariance != future actuality`;
- `measurement covariance != modal/ontological identity`;
- `perspective-invariant future probabilities != proof of eternalism`;
- `measurement covariance != refutation of ontological becoming`;
- `typed-resource necessity != metaphysical fundamentality`;
- `parametrized covariance precursor != general relativity`;
- `finite-model success != empirical discovery`.
