# Stage 12B Result — Dirac/Relational Physical-Orbit Discrimination

Status: **Stage 12B completed; criteria 17–23 satisfied.**

Bounded result:

`Stage 12B Dirac/relational physical-orbit discrimination on the frozen finite family = established`.

## Executable evidence

- physical orbits: **4**;
- independently reconstructed gauge-representative Dirac estimates: **20**;
- independently reconstructed external-parameterization Dirac estimates: **16**;
- unordered distinct physical-orbit comparisons: **6**, all kept distinct by the full `(Q_D,P_D)` pair;
- relational test readings: `tau in {-1.25,-0.25,0.75,1.50}`;
- `q(T=tau)=Q_D+P_D tau` evaluations: **144**;
- `dq/dT=P_D` finite-difference evaluations: **232**;
- same-P/different-Q control (`omega_alpha`/`omega_beta`): rejected as same orbit;
- same-Q/different-P control (`omega_alpha`/`omega_gamma`): rejected as same orbit;
- equal-T cross-orbit coincidences: **30**, rejected as sufficient identity;
- equal-q cross-orbit coincidences: **2**, rejected as sufficient identity;
- equal-raw-lambda cross-orbit coincidences: **312**, rejected as sufficient identity;
- false-match classification: `false_positive_rejected`.

All positive numerical residuals are required by the executable diagnostics to remain within `1e-10`.

## What this establishes

Within the frozen finite model family, the full pair

`(Q_D,P_D)=(q-pT,p)`

is representative-independent on each declared physical orbit and discriminates the four canonical physical orbits. At the same time,

`q(T=tau)=Q_D+P_D tau`

and

`dq/dT=P_D`

remain nontrivial relational quantities.

Therefore the executable carrier realizes the conjunction

`Dirac-invariant orbit data + nontrivial relational change`.

## What it does not establish

`Dirac invariant != timeless ontology by definition`.

`Dirac-invariant data + relational change != proof of eternalism`.

`relational change != ontological becoming by definition`.

`different physical orbit != later event on one orbit`.

`gauge quotient != elimination of physical change`.

`full-Dirac-pair discrimination in this finite family != universal orbit-classification theorem`.

`multi-orbit gauge covariance != general covariance`.

Next: **Stage 12C — typed gauge atlas, quotient, and descent of relational structure.**
