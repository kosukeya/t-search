# Stage 1A Baseline Result

Status: **sanity-check result, not a metaphysical conclusion**.

## Protocol

This run implements `docs/stage1_protocol.md` Stage 1A without adding Potentiality, records, entropy, or quantum structure.

Global object:

`B_1 = (E, C)`

with:

- `E = {a,b,c,d,e,f}`
- `C = {(a,b),(a,c),(b,d),(c,d),(d,e),(d,f)}`

Each local view is:

`V_e = (id_e, Pred_1(e), Succ_1(e))`.

The tested round trip is:

`B_1 -> {V_e} -> B_1_hat`.

## Observed result

The canonical run produced:

- events: 6
- direct edges: 6
- DAG check: true
- reachability pairs in the transitive closure: 13
- incoming/outgoing reports consistent: true
- labeled equality: true
- unlabeled directed-graph isomorphism: true
- reachability equality: true

Therefore, under the deliberately information-rich Stage 1A protocol:

`Glue(F(B_1)) = B_1`.

## Local views

- `a`: predecessors `{}`, successors `{b,c}`
- `b`: predecessors `{a}`, successors `{d}`
- `c`: predecessors `{a}`, successors `{d}`
- `d`: predecessors `{b,c}`, successors `{e,f}`
- `e`: predecessors `{d}`, successors `{}`
- `f`: predecessors `{d}`, successors `{}`

## Property classification

### Local observable

Available directly in a single Stage 1A view:

- the view's event ID;
- immediate predecessor IDs;
- immediate successor IDs.

### Reconstructible from the full family

- the full direct-edge set `C`;
- the reachability relation `prec` after transitive closure.

### Strict invariant

None is claimed from Stage 1A alone. The round trip is intentionally easy because global event IDs and both incoming/outgoing reports are retained.

### Ambiguous / lost

None in the baseline protocol. Stage 1B will deliberately remove information to determine when reconstruction becomes ambiguous or impossible.

## Consistency guard

A negative test removes the incoming report `b -> d` from `V_d` while leaving the outgoing report in `V_b`. The consistency checker detects the mismatch and `Glue` raises an error instead of silently selecting one report.

This confirms that successful reconstruction depends on a mutually consistent family of local views, as required by the protocol.

## What Stage 1A establishes

Stage 1A establishes only that the chosen projection/gluing machinery faithfully round-trips the canonical labeled DAG when enough information is retained.

It does **not** establish that:

- block-like and becoming-like descriptions are physically equivalent;
- reachability is a fundamental temporal invariant;
- Future Potential is ontic or epistemic;
- local becoming is generated by records;
- the program has found a new physical law.

## Next question

Proceed to Stage 1B and intentionally restrict the local views. The important question becomes:

> Which global relational properties remain uniquely reconstructible once privileged labels or redundant local information are removed?

## Simulation-order warning

The Python loop order used to produce and print local views is external implementation order only:

`simulation order != modeled temporal order`.

Modeled order is represented solely by `C` and its induced reachability relation `prec`.
