# Stage 4F — Operational and Negative Controls

Status: **completed**.

Clean PR merge-ref checkpoint including Stage 4F code/tests/experiment/notes/results:

`243 passed in 3.33s`.

## Operational Born consistency

Use the noncommuting projector:

`Pi_+=|+><+|`, `|+>=(|0>+|1>)/sqrt(2)`.

For the canonical equal-amplitude `d=4` physical state, both the global conditional probability and the local reduced-state Born rule give:

`[1/2, 1/4, 0, 1/4]`.

Thus the tested global and clock-relative descriptions agree operationally for an observable whose probability genuinely depends on the clock reading.

The equality is also included in the focused tests for generic complex physical coefficients and a `d=5` control.

## Constraint-violating control

For:

`|Phi_bad>=(|0>_C|0>_S+|0>_C|1>_S)/sqrt(2)`,

formal clock conditioning is well-defined, but the normalized formal conditional state is the same at all ideal DFT readings.

The expected Schrödinger evolution generated by `H_S` changes the relative phase, so the formal conditional sequence fails the expected relation for a nonzero clock step.

The physical global conditional-probability API rejects this state because it does not satisfy the zero-constraint condition.

Therefore:

`formal clock conditioning != physical Page--Wootters dynamics`.

## Single-energy physical control

For the constrained product state:

`|Psi_triv>=|1>_C|1>_S`,

clock-relative vectors change only by global phase:

`|psi_j>=exp(-i t_j)|1>_S`.

Between the first two `d=4` readings, the vector distance is nonzero (`sqrt(2)` analytically), while:

`pure-state fidelity = 1`,

and:

`density-matrix distance = 0`.

Hence:

`vector change != observable/ray change`,

and:

`constraint satisfaction != nontrivial relational ray evolution`.

## Wrong-clock-basis control

Conditioning on a clock energy basis state `|m>` acts on physical coefficient coordinates as:

`Q_m=|m><m|`.

For canonical `d=4`:

`rank(Q_m)=1`,

`nullity(Q_m)=3`.

Distinct physical coefficient vectors that agree only in sector `m` can therefore have the same energy-basis clock projection.

This contrasts with the Stage 4D DFT time-basis physical reduction `R_j`, which is invertible/isometric on the full matched-energy physical subspace.

At `d=5`, the corresponding energy-basis projection again has rank `1` and nullity `4`.

## Strongest supported Stage 4F statement

**within the ideal finite matched-energy Page--Wootters-style model, the global conditional Born rule and the clock-relative local Born rule agree for a nontrivial reading-dependent observable, while three negative controls identify important limits: violating the constraint destroys the expected conditional Schrödinger structure, a single-energy constrained state changes only by unobservable global phase, and conditioning on the clock energy basis is non-injective even on the physical subspace.**

These are representation/operational controls inside the toy model. They do not prove a unique physical clock basis, fundamental emergent time, ontological becoming, or a temporal arrow.

Focused Stage 4F tests: **12**.

Next: Stage 4G — robustness and synthesis.
