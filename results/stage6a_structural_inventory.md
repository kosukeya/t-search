# Stage 6A Results — Structural Inventory and Executable Adapters

Status: **completed**.

## Purpose

Stage 6A establishes a reusable executable evidence inventory for Stage 6. It does not yet decide the Stage 6B implication matrix.

The inventory is generated by:

`src/t_search/stage6_inventory.py`.

## Witness summary

### W1 — Stage 1 reconstruction / accessibility

Recomputed properties:

- complete labeled one-hop view family reconstructs the canonical block exactly;
- reconstructed reachability equals original reachability;
- canonical transitive closure contains `13` non-reflexive reachability pairs;
- event `e` is globally reachable from event `a`;
- event `e` is not contained in the declared one-hop local view at `a`;
- the local view at `a` has `2` immediate successors.

This is an executable reconstruction/accessibility witness, not yet a universal non-implication theorem.

### W2 — Stage 2 modal / operational witness

At Actuality prefix `('p','n')`:

- epistemic and ontic operational views are equal;
- Actuality agrees;
- immediate alternatives agree;
- next-event probabilities agree;
- each model has two live complete histories at the declared interface;
- the epistemic model contains a selected complete history;
- the ontic model has `0` selected-future-like dataclass fields;
- the epistemic and ontic Potentiality runtime types are different.

Thus the executable record retains both operational equality and model/modal distinction.

### W3 — Stage 3 order / record witness family

Recomputed orientations:

- `forward`: `lower-index`;
- `reversed`: `upper-index`;
- `symmetric`: `none`;
- `no-record`: `none`;
- `uniform-memory`: `none`.

Selected signed diagnostics:

- forward record score: `+1.0 bit`;
- reversed record score: `-1.0 bit`;
- forward accessibility score: `+0.5`;
- reversed accessibility score: `-0.5`;
- symmetric/no-record/uniform-memory record scores: `0` within frozen tolerance.

All declared control update maps used for the Stage 6A reversibility field are explicitly checked as bijective. The no-record control is checked using its actual `u_identity/U_scr` pair rather than inheriting canonical-map metadata.

### W4 — Stage 4 same-clock transition witness

For the canonical `d=4` family, exhaustive identity, inverse, composition, expected-transition, and unitarity residuals all remain below:

`1e-10`.

The witness records four clock readings and retains the semantic guard that reversible same-clock transition maps are not, by themselves, a record-defined temporal arrow.

### W5 — Stage 5 genuine clock / operational witness

Recomputed:

- all `162` ordered distinct-clock three-perspective composition routes;
- maximum cross-clock composition residual below `1e-10`;
- all ordered distinct-clock Born comparisons for all reading pairs with transported projectors;
- maximum Born-probability residual below `1e-10`;
- entanglement control at reading zero:
  - A clock: approximately `1 bit`;
  - B clock: approximately `0 bit`;
  - C clock: approximately `0 bit`.

The same witness therefore records both prediction-preserving perspective transport and a quantity that changes with reduced tensor-factor perspective.

## Inventory schema

Each witness exposes:

- source stage;
- declared model/interface domain;
- assumptions;
- semantic roles;
- named measured quantities;
- units where relevant;
- numerical tolerance where relevant.

The full inventory contains exactly one primary adapter per prior stage:

`W1,W2,W3,W4,W5`.

The JSON-friendly form remains serializable without replacing measured values by prose labels.

## Strongest supported Stage 6A statement

**Stages 1--5 now have one primary executable Stage 6 witness each, recomputed through the existing model APIs and represented in a common typed inventory that preserves domain, assumptions, roles, measurements, units, and tolerances. The inventory reproduces the previously established reconstruction/accessibility, modal/operational, record-orientation, same-clock-transition, and cross-clock/operational controls needed for the next independence analysis without hard-coding the intended Stage 6B answers.**

This is an evidence-engineering result for the research program, not a new physical result about time.

## Boundary

No Stage 6B implication is classified in this checkpoint.

In particular:

`executable witness available != universal implication decided`.

## Validation

Focused Stage 6A tests: **13**.

Initial code/test PR merge-ref checkpoint:

`352 passed in 18.57s`.

Final code checkpoint after the control-specific reversibility correction:

`352 passed in 16.12s`.
