# Stage 7D Results — Genuine Clock-Change Record Transport

Status: **completed for the declared Stage 7C forward qutrit history family**.

## Re-derived interacting perspectives

For each of the nine clock/readout nodes `(X,j)` with `X in {A,B,C}` and `j in {0,1,2}`, Stage 7D recomputes the reduction of the common Stage 7C modified physical space.

All nine reductions have rank `14` into an 18-dimensional reduced ambient space.

Each node therefore admits:

- a 14-dimensional image support `K_X,j^hist`;
- an exact support reconstruction;
- an exact cross-clock map through the common modified physical space.

The Stage 7A spectator support maps are not used as interacting maps.

## Ideal-clock deformation

The record interaction does not destroy the multi-clock structure, but it makes B/C clocks nonideal relative to the Stage 5/7A Euclidean-isometric criterion.

Canonical clock-reading probabilities:

- A: `[1/3, 1/3, 1/3]`;
- B: `[4/9, 5/18, 5/18]`;
- C: `[7/18, 7/18, 2/9]`.

The A reductions remain isometric within tolerance. B/C reductions remain full rank but have nonzero order-one isometry residuals; the largest reduction-coordinate condition number is approximately `5.828427`.

## Induced metric atlas

Writing the interacting reduction in orthonormal image coordinates as

`y_X,j=C_X,j c`, 

the induced physical metric is

`G_X,j=C_X,j^{-dagger} C_X,j^{-1}`.

The re-derived genuine clock change

`S_{Y<-X}=C_Y C_X^{-1}`

satisfies

`S^dagger G_Y S = G_X`

for all tested distinct-clock/readout pairs.

The largest Euclidean-unitarity residual is strongly nonzero, while the induced-metric covariance residual remains within numerical tolerance.

Thus the surviving perspective structure is metric-preserving rather than Euclidean-unitary.

## Common physical record observables

The Stage 7C lower-event target, upper-event target, and memory readout projectors are first represented on the common modified physical space and then reduced into each clock chart.

In a chart:

`O_X=C_X O_phys C_X^{-1}`.

These operators are:

- idempotent;
- mutually commuting where jointly read;
- self-adjoint with respect to `G_X`.

Corresponding observable transport holds across all **54 directed distinct-clock/readout comparisons**.

## Orientation-preserving chi

Explicit event correspondence:

`e0 -> e0`, `e1 -> e1`, `e2 -> e2`.

For all nine clock/readout nodes:

- `I(M;Q_e0)=1 bit`;
- `I(M;Q_e2)=0`;
- lower decoding accuracy `=1`;
- upper decoding accuracy `=1/2`;
- `A_R=+1`;
- `A_acc=+1/2`;
- orientation `lower-index`;
- `record_defined=true`.

Thus the Stage 7C record-defined orientation is covariant under the re-derived genuine clock changes when state, target observables, memory observables, induced metric, and event correspondence are transported consistently.

## Orientation-reversing chi

Explicit event correspondence:

`e0 -> e2`, `e1 -> e1`, `e2 -> e0`.

For all nine clock/readout nodes:

- `A_R=-1`;
- `A_acc=-1/2`;
- orientation `upper-index`.

The sign reversal is generated by the predeclared event correspondence, not by post-processing the preserving score.

## Negative controls

### Inherited Stage 7A spectator map

Canonical `A/e1 -> B/e0` residual:

`~0.5773502692`.

The old spectator map therefore fails to transport the interacting record-bearing state.

### Same bare observable

Leaving the source target matrix unchanged in the target chart produces a nonzero matrix correspondence residual and fails target-metric self-adjointness.

### Misdeclared chi

Using the reversing event mapping while declaring it preserving produces:

- record-score mismatch `2`;
- accessibility-score mismatch `1`.

All three controls are discriminated by the executable diagnostics.

## Exit-criteria consequence

Stage 7D satisfies the declared Stage 7 cross-perspective criteria 19–25 in the canonical family:

19. one common record-bearing physical construction is represented in multiple genuine clock perspectives;
20. source/target record observables are explicitly corresponded and transported;
21. event correspondence `chi` is explicit;
22. preserving transport preserves record statistics;
23. reversing correspondence obeys the predeclared sign rule;
24. same-bare-observable control fails;
25. wrong/misdeclared `chi` fails.

## Strongest bounded statement

**Within the declared finite Stage 7C qutrit history family, the record-bearing modified physical space admits nine full-rank internal-clock charts.  The record interaction deforms the ideal Stage 5/7A Euclidean-unitary atlas into a nonideal induced-metric atlas, but exact reconstruction, genuine cross-clock state transport, corresponding observable transport, and target-specific record covariance survive.  The forward record orientation is preserved by orientation-preserving event correspondence and changes sign under an explicitly declared orientation-reversing correspondence.**

This strengthens the finite-model `P-R` compatibility claim. It does not establish `P=R`, a fundamental time direction, general covariance, a thermodynamic arrow, ontological becoming, or phenomenal passage.

## Regression

Stage 7D adds **35 focused tests**.

Initial implementation-inclusive PR merge-ref regression:

`513 passed in 185.04s`.

## Next

Stage 7E — accessibility and partial-atlas record consistency.
