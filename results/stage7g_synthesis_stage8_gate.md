# Stage 7G Results — Synthesis and Stage 8 Gate

Status: **Stage 7 scientific synthesis completed.**

## Synthesis outcome

Executable choice:

**`strengthened`**

The strengthened scope is the finite-model `P/O/R` core plus the tested `Xi_PR` compatibility relation.

Stage 7 does not validate the full `O/P/R/V/Xi` candidate as a fundamental ontology. `V` remains outside the shared constrained quantum construction and is therefore the next major pressure point.

## Decisive evidence

1. Stage 7C constructs an internally anchored target-specific quantum record history.
2. Stage 7D represents the same record-bearing physical construction in all A/B/C clock perspectives and preserves record statistics under correctly declared transport.
3. Stage 7E separates global record representation, reconstructibility, and local accessibility.
4. Stage 7F neutralizes record coupling while retaining the internal event anchor and full-rank multi-clock perspective structure:
   - 54 distinct-clock comparisons remain valid;
   - `A_R=0`;
   - `A_acc=0`;
   - `record_defined=false`.
5. Therefore `P + internal O => declared R` is refuted in the Stage 7 family.
6. Explicit cross-clock edge matrices are nevertheless reconstructible from the common physical carrier and per-perspective reductions.

Bounded consequence:

**The Stage 6 layered interpretation is strengthened for P/O/R, while the representation of P is refined: explicit edge matrices are derived in the current interface.**

## Stage 8 ranking

| Rank | Gate | Score | Main pressure |
|---:|---|---:|---|
| 1 | `quantum_potentiality` | 10 | V is the only explicit Stage 6 layer not yet inside the shared constrained quantum construction |
| 2 | `richer_causal_order` | 7 | O remains a deliberately minimal three-event anchor |
| 3 | `nonideal_povm_clocks` | 5 | true POVM/nonideal clock robustness remains incomplete |
| 4 | `parametrized_covariance_precursor` | 3 | important later, but currently introduces more confounds than discrimination |

Selected gate:

**Stage 8 — integrate explicit Potentiality / extension semantics `V` into the same constrained quantum construction.**

## Why this is the most discriminating next step

Stage 7 substantially removes the earlier model-separation objection for `P/O/R`.  `V` is now the conspicuous missing layer.

Stage 8 should test whether the Stage 2/6 distinction between epistemic selected-history uncertainty and ontic no-selected-future extension semantics can survive inside one physical constrained quantum construction while:

- genuine internal clock perspectives are present;
- corresponding observables are transported;
- record-bearing and no-record controls are available;
- local accessibility is explicit;
- operational predictions can be compared without declaring modal identity by fiat.

## Unresolved claims

The following remain `not_established`:

- physical clock change => temporal succession;
- record-defined direction => ontological future openness;
- record-defined direction => thermodynamic arrow;
- record-defined direction => phenomenal passage;
- perspective consistency => modal equivalence;
- Potentiality => phenomenal passage;
- finite P/O/R compatibility => general covariance;
- layered O/P/R/V/Xi structure => fundamental or unique ontology of time.

## Exit criteria

Criteria 1–35 are recomputed as satisfied by `stage7_synthesis.py`.

Criterion 36 remains external until:

- the final documentation-synchronized head passes full regression; and
- PR merge-readiness review finds no unresolved blocker.

## Validation checkpoint

Stage 7G adds 13 focused tests.

Implementation-inclusive pre-documentation regression:

**`561 passed in 159.86s`**.

## Strongest bounded statement

**Within the declared finite constrained quantum family, P, internal O, and target-specific R can coexist with nontrivial covariance while P plus internal O can also be retained in a no-record countermodel where R vanishes.  This strengthens a layered P/O/R interpretation without establishing metaphysical irreducibility.  The highest-value next test is to integrate the still-separate V/Potentiality layer into the same quantum construction.**
